import { describe, test } from 'vitest';

// AUTO-GENERATED from docs/user-stories/admin/ee-home.md by `strapi user-stories:sync-e2e`.
// One test() per acceptance criterion (Given/When/Then). Replace each test.todo with a real
// implementation using the Vitest browser fixture — see the imports below and
// tests/e2e/tests/admin/login.vitest.spec.ts for the canonical shape.
//
// import { createBrowserSession, closeBrowserSession, type BrowserSession } from '../../../vitest/browser-fixture';
// import { expect } from '../../../vitest/expect';

describe('Homepage Last Activity Widget (Enterprise Edition)', () => {
  describe('See recent activity on the homepage', () => {
    // Given I have logged in
    // When I view the homepage
    // Then a "Last activity" widget is visible
    test.todo('AC1.1 — I view the homepage');

    // Given the "Last activity" widget is visible
    // When I open Content Manager → Article, edit the "West Ham post match analysis" entry's title to "West Ham pre match pep talk", and Save
    // Then I see a "Saved document" confirmation
    test.todo('AC1.2 — I open Content Manager → Article, edit the "West Ham post match analy…');

    // Given I have just saved that edit
    // When I return to Home
    // Then the latest activity row updates to show an "update entry (article)" action
    test.todo('AC1.3 — I return to Home');
  });
});
