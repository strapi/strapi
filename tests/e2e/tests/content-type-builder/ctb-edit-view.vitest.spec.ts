import { describe, test } from 'vitest';

// AUTO-GENERATED from docs/user-stories/content-type-builder/ctb-edit-view.md by `strapi user-stories:sync-e2e`.
// One test() per acceptance criterion (Given/When/Then). Replace each test.todo with a real
// implementation using the Vitest browser fixture — see the imports below and
// tests/e2e/tests/admin/login.vitest.spec.ts for the canonical shape.
//
// import { createBrowserSession, closeBrowserSession, type BrowserSession } from '../../vitest/browser-fixture';
// import { expect } from '../../vitest/expect';

describe('Content-Type Builder Edit View', () => {
  describe('Navigate to the Edit View and see content type fields', () => {
    // Given the Content-Type Builder is open
    // When the user selects the "Shop" single type
    // Then the "Shop" edit view is shown
    test.todo('AC1.1 — the user selects the "Shop" single type');

    // Given the "Shop" edit view is shown
    // When the user inspects it
    // Then two "Add another field to this component" buttons are visible
    test.todo('AC1.2 — the user inspects it');
  });
});
