import { describe, test } from 'vitest';

// AUTO-GENERATED from docs/user-stories/content-type-builder/guided-tour.md by `strapi user-stories:sync-e2e`.
// One test() per acceptance criterion (Given/When/Then). Replace each test.todo with a real
// implementation using the Vitest browser fixture — see the imports below and
// tests/e2e/tests/admin/login.vitest.spec.ts for the canonical shape.
//
// import { createBrowserSession, closeBrowserSession, type BrowserSession } from '../../vitest/browser-fixture';
// import { expect } from '../../vitest/expect';

describe('Content-Type Builder Guided Tour (AI Chat)', () => {
  describe('See the AI-enabled Content-Type Builder guided tour', () => {
    // Given the guided tour is enabled (EE only)
    // When the user starts the "Create your schema" guided tour step
    // Then a "Welcome to the Content-Type Builder!" dialog is shown
    test.todo('AC1.1 — the user starts the "Create your schema" guided tour step');

    // Given the "cms-ai" feature is enabled and the welcome dialog is shown
    // When the user clicks "Next"
    // Then a "Time to get started!" dialog is shown
    test.todo('AC1.2 — the user clicks "Next"');

    // Given the "Time to get started!" dialog is shown
    // When the user clicks "Next" again
    // Then the AI chat opens, shown by the "Ask Strapi AI..." textbox becoming visible
    test.todo('AC1.3 — the user clicks "Next" again');
  });
});
