import { describe, test } from 'vitest';

// AUTO-GENERATED from docs/user-stories/i18n/bulk-locale-modal.md by `strapi user-stories:sync-e2e`.
// One test() per acceptance criterion (Given/When/Then). Replace each test.todo with a real
// implementation using the Vitest browser fixture — see the imports below and
// tests/e2e/tests/admin/login.vitest.spec.ts for the canonical shape.
//
// import { createBrowserSession, closeBrowserSession, type BrowserSession } from '../../vitest/browser-fixture';
// import { expect } from '../../vitest/expect';

describe('i18n Bulk Locale Modal', () => {
  describe('Bulk locale modal lists all locales after creating a new localization', () => {
    // Then a "Published" confirmation is shown
    test.todo('AC1.1 — a "Published" confirmation is shown');

    // Then the view switches to that locale
    test.todo('AC1.2 — the view switches to that locale');

    // Then the bulk locale modal opens
    test.todo('AC1.3 — the bulk locale modal opens');

    // Then a "Successfully unpublished" confirmation is shown
    test.todo('AC1.4 — a "Successfully unpublished" confirmation is shown');
  });
});
