import { describe, test } from 'vitest';

// AUTO-GENERATED from docs/user-stories/search/content-type.md by `strapi user-stories:sync-e2e`.
// One test() per acceptance criterion (Given/When/Then). Replace each test.todo with a real
// implementation using the Vitest browser fixture — see the imports below and
// tests/e2e/tests/admin/login.vitest.spec.ts for the canonical shape.
//
// import { createBrowserSession, closeBrowserSession, type BrowserSession } from '../../vitest/browser-fixture';
// import { expect } from '../../vitest/expect';

describe('Content Type Search', () => {
  describe('Search a content type list view by name', () => {
    // Given the Products list view with a new entry created with a given name, saved, and navigated back to the list
    // When the search input is opened, the search term is typed, and Enter is pressed
    // Then the table is filtered
    test.todo('AC1.1 — the search input is opened, the search term is typed, and Enter is pr…');
  });
});
