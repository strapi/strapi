import { describe, test } from 'vitest';

// AUTO-GENERATED from docs/user-stories/content-manager/relations-on-the-fly/create-relation-and-publish.md by `strapi user-stories:sync-e2e`.
// One test() per acceptance criterion (Given/When/Then). Replace each test.todo with a real
// implementation using the Vitest browser fixture — see the imports below and
// tests/e2e/tests/admin/login.vitest.spec.ts for the canonical shape.
//
// import { createBrowserSession, closeBrowserSession, type BrowserSession } from '../../../vitest/browser-fixture';
// import { expect } from '../../../vitest/expect';

describe('Relations on the Fly - Create a Relation and Publish', () => {
  describe('Create a new relation, publish it, and have it added to the parent document', () => {
    // Then the relation modal opens with the new relation's `name` field empty
    test.todo("AC1.1 — the relation modal opens with the new relation's `name` field empty");

    // Then the name "Mr. Fred Passo" is kept and a "Published" status is shown
    test.todo('AC1.2 — the name "Mr. Fred Passo" is kept and a "Published" status is shown');

    // Then "Mr. Fred Passo" is shown as a related author button on the parent Article
    test.todo('AC1.3 — "Mr. Fred Passo" is shown as a related author button on the parent Ar…');
  });
});
