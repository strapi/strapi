import { describe, test } from 'vitest';

// AUTO-GENERATED from docs/user-stories/content-manager/relations-on-the-fly/create-relation-and-save.md by `strapi user-stories:sync-e2e`.
// One test() per acceptance criterion (Given/When/Then). Replace each test.todo with a real
// implementation using the Vitest browser fixture — see the imports below and
// tests/e2e/tests/admin/login.vitest.spec.ts for the canonical shape.
//
// import { createBrowserSession, closeBrowserSession, type BrowserSession } from '../../../vitest/browser-fixture';
// import { expect } from '../../../vitest/expect';

describe('Relations on the Fly - Create a Relation and Save', () => {
  describe('Create a new relation, save it as draft, and have it added to the parent document', () => {
    // Then the relation modal opens showing the "Create a relation" banner and an "Untitled" heading
    test.todo('AC1.1 — the relation modal opens showing the "Create a relation" banner and a…');

    // Then the name "Mr. Plop" is kept and a "Draft" status is shown
    test.todo('AC1.2 — the name "Mr. Plop" is kept and a "Draft" status is shown');

    // Then the "Create a relation" modal is hidden and "Mr. Plop" is shown as a related author button on the parent Article
    test.todo('AC1.3 — the "Create a relation" modal is hidden and "Mr. Plop" is shown as a…');
  });
});
