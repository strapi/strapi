import { describe, test } from 'vitest';

// AUTO-GENERATED from docs/user-stories/content-manager/relations-on-the-fly/create-relation-in-new-component-and-save.md by `strapi user-stories:sync-e2e`.
// One test() per acceptance criterion (Given/When/Then). Replace each test.todo with a real
// implementation using the Vitest browser fixture — see the imports below and
// tests/e2e/tests/admin/login.vitest.spec.ts for the canonical shape.
//
// import { createBrowserSession, closeBrowserSession, type BrowserSession } from '../../../vitest/browser-fixture';
// import { expect } from '../../../vitest/expect';

describe('Relations on the Fly - Create a Relation Inside a New Component and Save', () => {
  describe('Create a relation inside a newly added component and save it', () => {
    // Then the relation modal opens showing "Create a relation" and an "Untitled" heading
    test.todo('AC1.1 — the relation modal opens showing "Create a relation" and an "Untitled…');

    // Then the name is kept and a "Draft" status is shown
    test.todo('AC1.2 — the name is kept and a "Draft" status is shown');

    // Then "Nike Zoom Kd Iv Gold C800" is shown as a related product button on the new component
    test.todo('AC1.3 — "Nike Zoom Kd Iv Gold C800" is shown as a related product button on t…');
  });
});
