import { describe, test } from 'vitest';

// AUTO-GENERATED from docs/user-stories/content-manager/edit-view/collection-type-edit-view-errors.md by `strapi user-stories:sync-e2e`.
// One test() per acceptance criterion (Given/When/Then). Replace each test.todo with a real
// implementation using the Vitest browser fixture — see the imports below and
// tests/e2e/tests/admin/login.vitest.spec.ts for the canonical shape.
//
// import { createBrowserSession, closeBrowserSession, type BrowserSession } from '../../../vitest/browser-fixture';
// import { expect } from '../../../vitest/expect';

describe('Collection Type Edit View - Validation and Permission Errors', () => {
  describe('See a validation error when publishing with an unfilled required component', () => {
    // Then the error "There are validation errors in your document. Please fix them before saving." is displayed
    test.todo('AC1.1 — the error "There are validation errors in your document. Please fix t…');
  });

  describe('See a permission error when a required field is not readable', () => {
    // Then the error "Your current permissions prevent access to certain required fields. Please request access from an administrator to proceed." is displayed
    test.todo('AC2.1 — the error "Your current permissions prevent access to certain require…');
  });
});
