import { describe, test } from 'vitest';

// AUTO-GENERATED from docs/user-stories/content-manager/conditional-fields/boolean-conditional-select-relation.md by `strapi user-stories:sync-e2e`.
// One test() per acceptance criterion (Given/When/Then). Replace each test.todo with a real
// implementation using the Vitest browser fixture — see the imports below and
// tests/e2e/tests/admin/login.vitest.spec.ts for the canonical shape.
//
// import { createBrowserSession, closeBrowserSession, type BrowserSession } from '../../../vitest/browser-fixture';
// import { expect } from '../../../vitest/expect';

describe('Selecting and Saving Relations in Boolean-Controlled Conditional Fields', () => {
  describe('Select a relation inside a conditional field and save it', () => {
    // Then a "Saved Document" confirmation is shown
    test.todo('AC1.1 — a "Saved Document" confirmation is shown');

    // Then the `bestFriendCats` relation field is visible
    test.todo('AC1.2 — the `bestFriendCats` relation field is visible');

    // Then that cat is added as a related entry
    test.todo('AC1.3 — that cat is added as a related entry');

    // Then a "Saved Document" confirmation is shown
    test.todo('AC1.4 — a "Saved Document" confirmation is shown');
  });
});
