{
  "name": "dts-tests",
  "private": true,
  "description": "Public API type tests. In userland mode (yarn test:dts:userland) this manifest becomes the one of an isolated application, with workspace packages installed from packed tarballs. Only declare what the tests import.",
  "devDependencies": {
    "@strapi/core": "workspace:*",
    "@strapi/types": "workspace:*",
    "@types/node": "20.19.41",
    "typescript": "5.9.3",
    "vitest": "catalog:"
  }
}
