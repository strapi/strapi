import styled from 'styled-components';

const ControlsWrapper = styled.div`
  display: flex;
  flex-wrap: wrap;
  padding-top: 1px;
  > div {
    margin-right: 10px;
    margin-bottom: 6px;
  }
`;

export default ControlsWrapper;
