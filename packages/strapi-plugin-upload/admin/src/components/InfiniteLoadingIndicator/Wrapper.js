import styled from 'styled-components';

const Wrapper = styled.div`
  position: absolute;
  top: calc(50% - 2px);
  left: calc(50% - 22%);
  width: 44%;
  text-align: center;
`;

export default Wrapper;
