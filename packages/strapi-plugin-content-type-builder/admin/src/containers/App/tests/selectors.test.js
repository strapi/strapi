// import { fromJS } from 'immutable';
// import { makeSelectAppDomain } from '../selectors';

// const selector = makeSelectAppDomain();

describe('makeSelectAppDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
