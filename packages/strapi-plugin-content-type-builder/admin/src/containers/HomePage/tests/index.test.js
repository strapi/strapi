// import React from 'react';
// import { shallow } from 'enzyme';

// import { HomePage } from '../index';

describe('<HomePage />', () => {

  // let props;

  beforeEach(() => {
    // props = {
    //   models: [],
    // };
  });

  it('should not crash', () => {
    // shallow(<HomePage {...props} />);
  });
});
