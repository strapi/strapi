import styled from 'styled-components';
import GrabCompo from './GrabCompo';

const GrabIcon = styled(GrabCompo)`
  > g {
    fill: '#B3B5B9';

    &:hover {
      fill: '#AED4FB';
    }
  }
`;

export default GrabIcon;
