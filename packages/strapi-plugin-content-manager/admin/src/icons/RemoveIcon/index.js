import styled from 'styled-components';
import RemoveCompo from './RemoveCompo';

const RemoveIcon = styled(RemoveCompo)`
  > path {
    fill: #b3b5b9;

    &:hover {
      fill: #aed4fb;
    }
  }
`;

export default RemoveIcon;
