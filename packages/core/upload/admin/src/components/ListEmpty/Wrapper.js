import styled from 'styled-components';

const Wrapper = styled.div`
  position: relative;
  margin-top: 19px;
  padding: 0;
  .btn-wrapper {
    position: absolute;
    top: 109px;
    width: 100%;
    text-align: center;
  }
`;

export default Wrapper;
