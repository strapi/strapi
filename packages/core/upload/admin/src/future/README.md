# Future

The future folder houses all the new code for the Media Library redesign project. The content of this folder will eventually replace its counterpart in the `src` folder. This means folders like `components`, `hooks`, and `pages` MUST NOT be referenced outside the future folder.

## Data fetching

All data fetching MUST be done with Redux Toolkit Query instead of Tanstack Query.
