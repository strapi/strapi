/**
 * `@strapi/strapi/attributes` — the typed attribute builders.
 *
 * @example
 * ```ts
 * import * as is from '@strapi/strapi/attributes';
 * const title = is.string({ required: true });
 * ```
 */
export * from '@strapi/core/attributes';
