describe('Local Strapi Source Provider - Entities Streaming', () => {
  describe('Create Links Stream', () => {
    test.todo('should return an empty stream if no content types ');

    test.todo(
      'should return an empty stream if content types but without links (but has components, dynamic zones) '
    );

    test.todo('Should return an empty stream if no links have been created for the relations');

    test.todo(
      'Should return a populated stream if there are content types with actual relation items'
    );
  });
});
