<!--- RelationsInput.stories.mdx --->

import { Meta, ArgsTable, Canvas, Story } from '@storybook/addon-docs';
import { BaseLink } from '@strapi/design-system/BaseLink';
import { Typography } from '@strapi/design-system/Typography';
import { Badge } from '@strapi/design-system/Badge';
import { Icon } from '@strapi/design-system/Icon';
import Cross from '@strapi/icons/Cross';
import { ShadowList, RelationItem } from './index';
import { relations } from './relationsDataMock';

<Meta title="components/RelationsInput" />

# RelationsInput

WIP

## Usage

<Canvas>
  <Story name="base">
    {() => {
      return (
        <Main>
          <ShadowList overflowing="both">
            {relations.map(relation => {
              const { title, status } = relation;
              const variant = status === 'published' ? 'success' : 'secondary';
              return (
                <RelationItem endAction={<button type="button"><Icon width="12px" as={Cross} /></button>}>
                  <BaseLink href="/">
                    <Typography textColor="primary600">{title}</Typography>
                  </BaseLink>
                  <Badge
                    borderSize={1}
                    borderColor={`${variant}200`}
                    backgroundColor={`${variant}100`}
                    textColor={`${variant}700`}
                  >
                    {status}
                  </Badge>
                </RelationItem>
              );
            })}
          </ShadowList>
        </Main>
      );
    }}
  </Story>
</Canvas>

### Props

<ArgsTable of={RelationItem} />
