{
  "extends": "tsconfig/server.json",
  "include": ["./src", "../shared"],
  "compilerOptions": {
    // Resolve our own contracts from source: `@strapi/strapi/strict-types` would load our dist instead.
    "paths": {
      "@strapi/email/strapi-server": ["./src/index.ts"]
    }
  }
}
