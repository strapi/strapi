{
  "routes": [
    {
      "method": "GET",
      "path": "/{{pluralize id}}",
      "handler": "{{id}}.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/{{pluralize id}}",
      "handler": "{{id}}.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/{{pluralize id}}/",
      "handler": "{{id}}.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
