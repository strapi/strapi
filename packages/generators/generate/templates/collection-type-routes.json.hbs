{
  "routes": [
    {
      "method": "GET",
      "path": "/{{pluralize id}}",
      "handler": "{{id}}.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/{{pluralize id}}/:id",
      "handler": "{{id}}.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/{{pluralize id}}",
      "handler": "{{id}}.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/{{pluralize id}}/:id",
      "handler": "{{id}}.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/{{pluralize id}}/:id",
      "handler": "{{id}}.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
