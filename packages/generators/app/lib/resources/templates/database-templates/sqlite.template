module.exports = ({ env }) => ({
  connection: {
    client: 'sqlite',
    connection: {
      filename: env('DATABASE_FILENAME', '<%= connection.filename %>'),
    },
    useNullAsDefault: true,
  },
});
