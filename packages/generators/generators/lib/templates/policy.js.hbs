'use strict';

/**
 * `{{id}}` policy.
 */

module.exports = async (ctx) => {
  // Add your own logic here.
  console.log('In {{id}} policy.');

  const canDoSomething = true;

  if (canDoSomething) {
    return true;
  }

  return false;
};
