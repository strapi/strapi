import type { TaskOptions } from '../types';

export const latest = async (options: TaskOptions) => {
  // find current version
  // find all versions available after the current, group by major
  // loop until no next major is found
  //   next
  //   fix-current
  console.log('not implemented, upgrade to the latest version', options);
};
