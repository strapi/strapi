import styled from 'styled-components';

const Wrapper = styled.div`
  position: relative;
  padding-bottom: 18px;
  label {
    display: block;
    margin-bottom: 1rem;
  }
`;

export default Wrapper;
